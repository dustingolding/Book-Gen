{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "slw-dailycast.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "slw-dailycast.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
